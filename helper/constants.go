package helper

const (
	FRAME_DEFAULT = 100 // 默认每帧 100ms

	OS_SCRIPT_EMPTY = "empty"
	OS_SCRIPT_BASH  = "bash"

	DEAMON_ENABLE  = "enable"
	DEAMON_DISABLE = "disable"

	DEAMON_CONFIG_SERV = "reload-yaml"
)
